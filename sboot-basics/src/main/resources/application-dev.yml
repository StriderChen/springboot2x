server:
  port: 8888
  servlet:
    context-path: /stbasics
spring:
  thymeleaf:
    # 关闭缓存
    cache: false
    mode: html
    # 配置视图前缀路径
    prefix: classpath:/templates/
    # 配置视图模板后缀
    suffix: .html

